# Add XXX dependency
poetry add XXX

# Run backend with docker compose
# For m1: export DOCKER_DEFAULT_PLATFORM=linux/amd64                                                                   
docker-compose up --build --abort-on-container-exit


# Test backend with docker compose
docker-compose up -d --build
docker-compose exec backend  pytest --cov=backend --html=test/report/report.html -W ignore::DeprecationWarning
docker-compose stop


# Test backend at root dir
./app/run_test.sh

# add sth
